---
# tasks file for roles/base
- name: Update system
  apt:
    update_cache: yes
    upgrade: dist

- name: Install base packages
  apt:
    name:
      - git
      - curl
      - vim
      - htop
      - ca-certificates
      - gnupg
      - lsb-release
    state: present
